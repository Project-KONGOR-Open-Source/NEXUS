@inherits LayoutComponentBase

<MudThemeProvider @ref="ThemeProvider" Theme="Theme" IsDarkMode="IsDarkMode" />
<MudPopoverProvider />
<MudDialogProvider />
<MudSnackbarProvider />

<MudLayout>
    <MudAppBar Elevation="1" Fixed="true">
        <MudContainer MaxWidth="MaxWidth.ExtraLarge" Class="d-flex align-center pa-0">
            <MudIconButton Icon="@Icons.Material.Filled.Menu" Color="Color.Inherit" Edge="Edge.Start" OnClick="ToggleDrawer" Class="d-flex d-md-none" />

            <a href="/" class="d-flex align-center text-decoration-none mud-inherit-text">
                <MudImage Src="images/logo.svg" Alt="Project KONGOR" Width="40" Height="40" Class="mr-2" />
                <MudText Typo="Typo.h6" Class="font-scurlock">Project KONGOR</MudText>
            </a>

            <MudSpacer />

            <MudStack Row="true" Spacing="1" Class="d-none d-md-flex align-center">
                <MudButton Href="/" Color="Color.Inherit" Variant="Variant.Text">Home</MudButton>

                <MudMenu Label="Community" Color="Color.Inherit" Variant="Variant.Text" EndIcon="@Icons.Material.Filled.KeyboardArrowDown" AnchorOrigin="Origin.BottomCenter" TransformOrigin="Origin.TopCenter">
                    <MudMenuItem Href="https://example.com/discord">Discord</MudMenuItem>
                    <MudMenuItem Href="https://example.com/reddit">Reddit</MudMenuItem>
                </MudMenu>

                <MudButton Href="/downloads" Color="Color.Inherit" Variant="Variant.Text">Downloads</MudButton>

                <MudButton Href="https://github.com/Project-KONGOR-Open-Source" Target="_blank" Color="Color.Inherit" Variant="Variant.Text">Source Code</MudButton>

                <MudButton Href="https://codex.kongor.net" Target="_blank" Color="Color.Inherit" Variant="Variant.Text">Knowledge Base</MudButton>
            </MudStack>

            <MudSpacer />

            <MudStack Row="true" Spacing="1" Class="align-center">
                <AuthorizeView>
                    <NotAuthorized>
                        <MudButton Href="/register" Color="Color.Primary" Variant="Variant.Filled" Class="d-none d-md-flex">Register Account</MudButton>
                        <MudButton Href="/login" Color="Color.Inherit" Variant="Variant.Text" Class="d-none d-md-flex">Log In</MudButton>
                    </NotAuthorized>
                    <Authorized>
                        <MudButton Href="/account" Color="Color.Primary" Variant="Variant.Filled" Class="d-none d-md-flex">Manage Account</MudButton>
                        <MudButton Color="Color.Inherit" Variant="Variant.Text" OnClick="HandleLogOut" Class="d-none d-md-flex">Log Out</MudButton>
                    </Authorized>
                </AuthorizeView>

                <MudDivider Vertical="true" FlexItem="true" Class="mx-2 my-4 d-none d-md-flex" />

                <MudIconButton Icon="@ThemeIcon" Color="Color.Inherit" OnClick="ToggleTheme" />
            </MudStack>
        </MudContainer>
    </MudAppBar>

    <MudDrawer @bind-Open="DrawerOpen" Elevation="1" Variant="DrawerVariant.Temporary" Breakpoint="Breakpoint.Md">
        <MudNavMenu>
            <MudNavLink Href="/" Icon="@Icons.Material.Filled.Home">Home</MudNavLink>
            <MudNavGroup Title="Community" Icon="@Icons.Material.Filled.People" Expanded="false">
                <MudNavLink Href="https://example.com/discord">Discord</MudNavLink>
                <MudNavLink Href="https://example.com/reddit">Reddit</MudNavLink>
            </MudNavGroup>
            <MudNavLink Href="/downloads" Icon="@Icons.Material.Filled.Download">Downloads</MudNavLink>
            <MudNavLink Href="https://github.com/Project-KONGOR-Open-Source" Target="_blank" Icon="@Icons.Material.Filled.Code">Source Code</MudNavLink>
            <MudNavLink Href="https://codex.kongor.net" Target="_blank" Icon="@Icons.Material.Filled.MenuBook">Knowledge Base</MudNavLink>

            <MudDivider Class="my-2" />

            <AuthorizeView>
                <NotAuthorized>
                    <MudNavLink Href="/register" Icon="@Icons.Material.Filled.PersonAdd">Register Account</MudNavLink>
                    <MudNavLink Href="/login" Icon="@Icons.Material.Filled.Login">Log In</MudNavLink>
                </NotAuthorized>
                <Authorized>
                    <MudNavLink Href="/account" Icon="@Icons.Material.Filled.ManageAccounts">Manage Account</MudNavLink>
                    <MudNavLink Icon="@Icons.Material.Filled.Logout" OnClick="HandleLogOut">Log Out</MudNavLink>
                </Authorized>
            </AuthorizeView>
        </MudNavMenu>
    </MudDrawer>

    <MudMainContent Style="@ContentStyle">
        <div class="pt-6">
            <CascadingValue Name="IsDarkMode" Value="IsDarkMode">
                @Body
            </CascadingValue>
        </div>

        <MudStack Spacing="0" AlignItems="AlignItems.Center" Class="px-4 pt-4 pb-8 mt-6" Style="@FooterStyle">
            <MudText Typo="Typo.caption" Align="Align.Center">
                Copyright 2024 Project KONGOR. Contact: support@example.com
            </MudText>
            <MudText Typo="Typo.caption" Align="Align.Center" Color="Color.Secondary">
                Any and all of the Heroes Of Newerth intellectual property is owned exclusively by Garena.
            </MudText>
        </MudStack>
    </MudMainContent>
</MudLayout>

@code {

    [Inject]
    private NavigationManager NavigationManager { get; set; } = null!;

    [Inject]
    private PortalAuthenticationService AuthenticationService { get; set; } = null!;

    private static readonly MudTheme Theme = new()
    {
        Typography = new Typography
        {
            Default = new DefaultTypography
            {
                FontFamily = ["Righteous", "sans-serif"]
            }
        }
    };

    private MudThemeProvider ThemeProvider { get; set; } = null!;

    private bool IsDarkMode { get; set; } = true;

    private bool DrawerOpen { get; set; }

    private string ThemeIcon => IsDarkMode ? Icons.Material.Filled.LightMode : Icons.Material.Filled.DarkMode;

    private string ContentStyle => IsDarkMode
        ? "padding-top: 64px; min-height: 100vh; background: linear-gradient(rgba(20, 20, 30, 0.85), rgba(20, 20, 30, 0.95)), url('img/caldavar.jpg') center/cover no-repeat fixed;"
        : "padding-top: 64px; min-height: 100vh; background: linear-gradient(rgba(245, 245, 250, 0.85), rgba(245, 245, 250, 0.95)), url('img/caldavar.jpg') center/cover no-repeat fixed;";

    private string FooterStyle => IsDarkMode
        ? "background: rgba(0, 0, 0, 0);"
        : "background: rgba(255, 255, 255, 0);";

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            IsDarkMode = await ThemeProvider.GetSystemDarkModeAsync();
            StateHasChanged();
        }
    }

    private void ToggleTheme()
    {
        IsDarkMode = !IsDarkMode;
    }

    private void ToggleDrawer()
    {
        DrawerOpen = !DrawerOpen;
    }

    private async Task HandleLogOut()
    {
        await AuthenticationService.ClearToken();
        NavigationManager.NavigateTo("/", forceLoad: true);
    }
}
